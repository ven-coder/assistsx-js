# CHANGELOG 编写规则

## 目标读者

面向用 assistsx-js 写插件脚本的开发者，包括不熟悉底层实现的使用者。

## 写法要求

1. 用日常中文，避免 API 名、模块路径、TypeScript 类型名；**过期迁移条目除外**（见第 7 条）
2. 每条以 **新增**、**修复**、**优化**、**调整** 开头
3. 写「能做什么」，不写「调哪个方法」：  
   - ✅ 新增：插件内可使用数据库进行增删改查  
   - ❌ 新增：`db` 子模块封装 `assistsxDb` 桥接
4. 一条只说一件事
5. **版本号**：以 [npm 已发布版本](https://www.npmjs.com/package/assistsx-js) 为准；未发布到 npm 的改动一律写在 **待发布**，**不要**提前写 0.2.4、0.2.5 等未发布版本号
6. 已发布版本标题格式：`## x.y.z（YYYY-MM-DD）`
7. **过期（Deprecated）**：必须写出过期的方法/接口名，以及建议改用的方法/接口名，便于对照迁移：  
   - ✅ 调整：`AssistsX.loadWebViewOverlay` / `AssistsXAsync.closeOverlay` 已标记过期，请改用 `float.open` / `float.close`  
   - ❌ 调整：旧的网页浮层打开/关闭方式已标记过期，请改用浮窗能力

## 分类说明

| 分类 | 含义 |
|------|------|
| 新增 | 以前没有、现在脚本里能用的能力 |
| 修复 | 以前有问题、现在修好了 |
| 优化 | 使用体验、文档、兼容性变好 |
| 调整 | 行为或默认值变化（需说明影响） |

## 更新流程

1. 发版前：`git log` 梳理自 npm 上一版以来的提交
2. 改写成条目，写入 **待发布**
3. `npm publish` 成功后：将 **待发布** 移到新版本号下，并注明 npm 已可用

## 与文档的关系

详细 API 说明见 `docs/`；CHANGELOG 只写用户能感知的变化，不写参数列表。
